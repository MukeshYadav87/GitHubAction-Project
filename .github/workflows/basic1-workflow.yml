name: GitHub Actions Demo
run-name: Testing out GitHub Actions Event Trigger
on: 
  push:
    branches:
      - master
      - releases/**
  pull_request:
    branches:
      - master
      - releases/**
jobs:
  Basic-Workflow:
   runs-on: ubuntu-latest
   steps:
     - run: echo "job is executing as par of Basic workflow"
